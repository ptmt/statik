name: Publish Docker Image

on:
  push:
    branches:
      - main
    tags:
      - "v*"
  pull_request:
    branches:
      - main
    paths:
      - ".github/workflows/publish-docker.yml"
      - ".dockerignore"
      - "Dockerfile"
      - "kotlin"
      - "module.yaml"
      - "src/**"
  workflow_dispatch:

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}

permissions:
  contents: read
  packages: write

concurrency:
  group: docker-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v5

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '21'

      - name: Cache Amper dependencies
        uses: actions/cache@v4
        with:
          path: |
            ~/.cache/Kotlin
            ~/.cache/JetBrains/Kotlin
          key: ${{ runner.os }}-kotlin-${{ hashFiles('kotlin', 'module.yaml') }}
          restore-keys: |
            ${{ runner.os }}-kotlin-

      - name: Make kotlin executable
        run: chmod +x ./kotlin

      - name: Build executable jar
        run: |
          for attempt in 1 2 3; do
            ./kotlin task :statik:executableJarJvm && exit 0
            if [ "$attempt" = "3" ]; then
              exit 1
            fi
            sleep $((attempt * 30))
          done

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GitHub Container Registry
        if: github.event_name != 'pull_request'
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract Docker metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
          tags: |
            type=raw,value=latest,enable={{is_default_branch}}
            type=ref,event=tag
            type=sha,prefix=sha-
            type=semver,pattern={{version}}
            type=semver,pattern={{major}}.{{minor}}
          labels: |
            org.opencontainers.image.title=statik
            org.opencontainers.image.description=Static site generator and hosted CMS in Kotlin

      - name: Build and publish Docker image
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ./Dockerfile
          platforms: linux/amd64,linux/arm64
          pull: true
          push: ${{ github.event_name != 'pull_request' }}
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Verify tinyserve deploy token is set
        if: github.event_name != 'pull_request' && github.ref == 'refs/heads/main'
        env:
          TINYSERVE_DEPLOY_TOKEN: ${{ secrets.TINYSERVE_DEPLOY_TOKEN }}
        run: |
          if [ -z "$TINYSERVE_DEPLOY_TOKEN" ]; then
            echo "Missing required secret: TINYSERVE_DEPLOY_TOKEN"
            exit 1
          fi

      - name: Deploy to tinyserve
        if: github.event_name != 'pull_request' && github.ref == 'refs/heads/main'
        env:
          TINYSERVE_DEPLOY_TOKEN: ${{ secrets.TINYSERVE_DEPLOY_TOKEN }}
        run: |
          curl -X POST "https://api.tinyserve.org/webhook/deploy/statik-cms" \
            -H "Authorization: Bearer $TINYSERVE_DEPLOY_TOKEN"
